// Centralised, app-wide constants. Change values here instead of editing components.

export const API_BASE_URL = "https://dev.to/api";
export const PROFILE_BASE_URL = "https://dev.to";
export const AUTH_API_URL = "https://jsonplaceholder.typicode.com";

// Default page size for paginated lists (Feed, Users).
export const PAGE_SIZE = 10;

// Description preview length used by the SinglePost card.
export const POST_PREVIEW_LENGTH = 50;
