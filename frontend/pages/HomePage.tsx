import Feed from "../features/feed/Feed";

// Home is currently just the feed. If the dashboard grows, compose more
// feature components here rather than expanding Feed.
const HomePage = () => <Feed />;

export default HomePage;
